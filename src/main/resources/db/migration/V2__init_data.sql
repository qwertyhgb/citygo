-- =============================================================================
-- V2__init_data.sql
-- CityGo 种子数据初始化
--
-- 本迁移只插入角色的基础种子数据（USER / MERCHANT / ADMIN）。
-- 其余业务数据后续阶段通过接口创建，不在此处预置。
--
-- 使用 INSERT IGNORE 配合唯一索引 uk_code，保证脚本可重复执行且不产生冲突。
-- =============================================================================

INSERT IGNORE INTO `role` (`id`, `code`, `name`, `description`) VALUES
    (1, 'USER', '普通用户', '平台普通用户'),
    (2, 'MERCHANT', '商家', '商家用户（可管理店铺与商品）'),
    (3, 'ADMIN', '平台管理员', '平台运营管理员');